migrate_plus.data_parser.ndjson:
  type: migrate_plus_data_parser
  label: 'NDJSON parser'
  mapping:
    strict_mode:
      type: boolean
      label: 'Strict mode'
      description: 'When enabled (true), the migration will fail on the first malformed JSON line. When disabled (false), malformed lines are skipped with a warning logged.'
